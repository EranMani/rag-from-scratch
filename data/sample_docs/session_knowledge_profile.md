# Session Knowledge Profile

A knowledge profile stores lightweight signals learned during a session. For
example, the system can remember that a user prefers concise explanations,
already understands basic RAG, and is currently interested in LangGraph state
machines.

The next answer can use that profile to skip repeated background and focus on
the user's current learning goal.
