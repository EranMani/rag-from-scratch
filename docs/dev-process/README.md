# Development Process Archive

This folder keeps historical process material out of the repository root.

These files are not required to run the RAG demo or application. They document
the agentic workflow, planning history, token tracking, and team process used
while building the project.

Root loader files such as `AGENTS.md` and `CLAUDE.md` stay in the repository
root so agent tools can still discover them normally.
