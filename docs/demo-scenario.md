# Canonical Demo Scenario

This is the one path the repository should make obvious, runnable, and easy to
explain in an interview.

## Demo Promise

The demo proves that the system can:

- retrieve relevant RAG knowledge from local documents
- run the turn through a LangGraph state machine
- answer with retrieved context separated from generation
- infer lightweight knowledge-profile updates during the session
- reuse the updated session profile on the next response

## Scenario

The user is learning how LangGraph helps structure RAG applications. They first
ask a broad question, then ask a follow-up that should be shaped by the profile
signal extracted from the first turn.

### Turn 1

User message:

```text
I understand basic RAG, but I am confused about why LangGraph is useful. Can you explain it briefly?
```

Expected retrieval intent:

- find material about RAG pipeline stages
- find material about LangGraph/state-machine orchestration
- prefer concise explanatory context over deep implementation detail

Expected assistant behavior:

- explain that LangGraph makes the RAG flow explicit as state transitions
- mention retrieval, generation, assessment/profile-update as separate nodes
- keep the explanation concise because the user asked for brevity

Expected knowledge-profile update:

```text
- prefers concise explanations
- already understands basic RAG
- interested in LangGraph state machines
```

### Turn 2

User message:

```text
How would that profile update change the next answer?
```

Expected retrieval intent:

- retrieve material about session state and knowledge-profile updates
- connect profile state back to answer generation

Expected assistant behavior:

- explicitly use the session profile from turn 1
- answer concisely
- frame the explanation around LangGraph state passing and profile-aware prompts

Expected answer shape:

```text
Because your profile says you prefer concise explanations and already know basic
RAG, I would skip the introductory RAG recap and focus on the LangGraph state:
the retrieve node adds documents, the generate node uses those documents plus
your profile, and the update_profile node records new signals for the next turn.
```

## Transcript Contract

The runnable demo should print a transcript with these sections for each turn:

```text
User:
...

Retrieved context:
- ...
- ...

Assistant:
...

Knowledge profile:
- ...
```

Turn 2 must clearly show that the assistant is using the profile updates from
turn 1.

## Sample Transcript

If you do not want to run OpenAI or a local Ollama model, this transcript shows
the intended reviewer experience. Exact wording may vary because the assistant
answer is generated by an LLM, but the visible flow should remain the same:
Chroma retrieval, answer generation, profile extraction, profile merge, and
profile reuse on the next turn.

```text
RAG From Scratch - Real Chroma LLM Demo
============================================
Generation provider: openai
Generation model:    gpt-4o-mini
Embedding provider:  openai
Embedding model:     text-embedding-3-small
Flow: Chroma retrieval -> LLM generation -> profile update -> profile reuse

Turn 1
--------------------------------------------
User:
I understand basic RAG, but I am confused about why LangGraph is useful. Can you explain
it briefly?

Retrieved context from Chroma:
- LangGraph State Machines for RAG (data/sample_docs/langgraph_state_machine.md): LangGraph is useful for RAG because it turns a conversation pipeline into an explicit state machine...
- Retrieval and Generation Separation (data/sample_docs/retrieval_generation_separation.md): A clean RAG system keeps retrieval separate from generation...

Assistant generated by OpenAI:
LangGraph is useful for RAG because it transforms a conversation pipeline into an
explicit state machine. Each part of the process, retrieving documents, generating
answers, and updating the user profile, is handled by a separate node with a clear
input and output contract.

Knowledge profile before update:
- prior_knowledge: unknown
- current_interest: unknown
- communication_style: unknown

Knowledge profile after update:
- prior_knowledge: understands basic RAG
- current_interest: LangGraph state machines
- communication_style: prefers concise explanations

Profile updates this turn:
- prior_knowledge: understands basic RAG
- current_interest: LangGraph state machines
- communication_style: prefers concise explanations

Turn 2
--------------------------------------------
User:
How would that profile update change the next answer?

Retrieved context from Chroma:
- Session Knowledge Profile (data/sample_docs/session_knowledge_profile.md): A knowledge profile stores lightweight signals learned during a session...
- Retrieval and Generation Separation (data/sample_docs/retrieval_generation_separation.md): Retrieval finds relevant chunks; generation uses those chunks as grounding...

Assistant generated by OpenAI:
Because the profile says you already understand basic RAG and prefer concise
explanations, the next answer can skip generic RAG setup and focus directly on
LangGraph state machines. The retrieve node adds documents to state, the generate
node uses those documents plus the profile, and the update_profile node records
new signals for the following turn.

Knowledge profile before update:
- prior_knowledge: understands basic RAG
- current_interest: LangGraph state machines
- communication_style: prefers concise explanations

Knowledge profile after update:
- prior_knowledge: understands basic RAG
- current_interest: LangGraph state machines
- communication_style: prefers concise explanations

Profile updates this turn:
- no changes

Turn 3
--------------------------------------------
User:
I get LangGraph now. Switch to LangChain and explain chains like a game, with more
detail.

Retrieved context from Chroma:
- LangChain Chains (data/sample_docs/langchain_chains.md): LangChain chains compose model calls, prompts, retrievers, and parsers into reusable workflows...
- Retrieval and Generation Separation (data/sample_docs/retrieval_generation_separation.md): A clean RAG system keeps retrieval separate from generation...

Assistant generated by OpenAI:
Think of a LangChain chain like a game level. Each step has a job: one step collects
the player's quest text, another fetches useful lore from the knowledge base, another
hands that lore to the model, and a final step formats the result. Compared with the
LangGraph state-machine view, a chain feels more like a scripted route through known
steps, while LangGraph is better when the route needs branching, loops, or explicit
state transitions.

Knowledge profile before update:
- prior_knowledge: understands basic RAG
- current_interest: LangGraph state machines
- communication_style: prefers concise explanations

Knowledge profile after update:
- prior_knowledge: understands LangGraph state machines
- current_interest: LangChain chains
- communication_style: prefers detailed game-like explanations

Profile updates this turn:
- prior_knowledge: understands LangGraph state machines
- current_interest: LangChain chains
- communication_style: prefers detailed game-like explanations
```

## Implementation Notes

- The default demo should use bundled local sample documents.
- The primary path uses OpenAI embeddings and OpenAI generation when
  `OPENAI_API_KEY` is configured.
- If `OPENAI_API_KEY` is missing, the demo routes to the fallback path:
  local Hugging Face embeddings for Chroma retrieval and Ollama for generation.
- To intentionally show the fallback path even with an API key configured, set
  `DEMO_FORCE_OLLAMA=true`.
- The Ollama fallback requires a running local Ollama server and the configured
  model to be pulled first, for example: `ollama pull gemma3:4b`.
- The README, smoke tests, and demo entry point should all follow this scenario.
